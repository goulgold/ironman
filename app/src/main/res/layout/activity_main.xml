<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search in Youtube"
        android:id="@+id/search"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="135dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Fullscreen"
        android:id="@+id/fullscreen"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/search"
        android:layout_alignStart="@+id/search"
        android:layout_alignRight="@+id/search"
        android:layout_alignEnd="@+id/search" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SEARCH"
        android:id="@+id/ysearch"
        android:layout_marginTop="54dp"
        android:layout_below="@+id/fullscreen"
        android:layout_alignLeft="@+id/fullscreen"
        android:layout_alignStart="@+id/fullscreen"
        android:layout_alignRight="@+id/fullscreen"
        android:layout_alignEnd="@+id/fullscreen" />

</RelativeLayout>
